# Web Dev I - Lab Assignment 4  
## Prompt Quizzer – A Simple JavaScript Quiz Game

This project is a beginner-friendly JavaScript quiz game that runs entirely in the **browser console**.  
The quiz uses `prompt()` to take answers from the user and `alert()` to show feedback.

---

## 📌 Features
- Uses **arrays**, **functions**, **loops**, and **conditions**
- Stores questions as **objects** inside an array
- Uses `toLowerCase()` and `trim()` to check answers correctly
- Gives **immediate feedback** after every question
- Tracks score and displays **final result**
- Requires no HTML or webpage—only JavaScript

---

## 📁 Files Included
- `quiz.js` — Main quiz program  
- `README.md` — Documentation

---

## 🚀 How to Run the Quiz
1. Open your web browser (Chrome recommended)
2. Press:
   - **Ctrl + Shift + J** (Windows)  
   - **Cmd + Option + J** (Mac)
3. This opens the **Console**
4. Copy all the code from `quiz.js`
5. Paste it into the console and press **Enter**
6. The quiz will start automatically  
7. Answer each question using the popup boxes

---

## 🧠 What I Learned
- Creating arrays and objects in JavaScript  
- Using `for` loops to go through data  
- Using `prompt()` and `alert()`  
- Input cleaning using `trim()` and `toLowerCase()`  
- Writing simple functions  
- Using conditionals to check answers  
- Displaying scores and giving feedback  

---

## ✔ Requirements Completed
- Minimum 5 questions  
- Score tracking  
- Input validation  
- Immediate feedback  
- Runs in browser console  
- Organized code inside a function  
- README included  

---

## 📎 Submission
Push both files to your GitHub repo:

